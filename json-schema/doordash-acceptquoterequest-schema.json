{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/AcceptQuoteRequest",
  "title": "AcceptQuoteRequest",
  "type": "object",
  "properties": {
    "tip": {
      "type": "integer",
      "description": "Optional tip amount in cents to include with the delivery."
    }
  }
}